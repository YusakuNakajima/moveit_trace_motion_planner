# Launch Files

No generic launch file is provided in v0 because the planner package is robot
agnostic and the local environment does not include ROS 2. Use the example
pipeline YAML files under `examples/ur5e` and `examples/fr3` from the
corresponding robot MoveIt launch.
